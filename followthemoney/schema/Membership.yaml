Membership:
  label: "Membership"
  plural: "Memberships"
  extends: Interest
  matchable: false
  featured:
    - member
    - organization
    - role
  properties:
    member:
      label: "Member"
      type: entity
      schema: LegalEntity
      reverse:
        name: membershipMember
        label: "Memberships"
    organization:
      label: "Organization"
      type: entity
      schema: Organization
      reverse:
        name: membershipOrganization
        label: "Members"
