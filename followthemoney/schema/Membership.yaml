Membership:
  label: "Membership"
  extends: Interest
  icon: fa-id-badge
  properties:
    member:
      label: "Member"
      type: entity
      schema: LegalEntity
    organization:
      label: "Organization"
      type: entity
      schema: Organization
