Interest:
  label: "Interest"
  extends: Interval
  icon: fa-certificate
  properties:
    role:
      label: "Role"
    # level:
    #   label: "Level of interest" # direct/indirect
    # votes:
    #   label: "Number of votes"
