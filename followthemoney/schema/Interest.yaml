Interest:
  label: "Interest"
  extends: Interval
  icon: fa-certificate
  fuzzy: false
  properties:
    role:
      label: "Role"
    # level:
    #   label: "Level of interest" # direct/indirect
    # votes:
    #   label: "Number of votes"
