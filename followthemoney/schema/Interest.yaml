Interest:
  label: "Interest"
  extends: Interval
  matchable: false
  featured:
    - role
    - startDate
    - endDate
  properties:
    role:
      label: "Role"
    status:
      label: "Status"
    # level:
    #   label: "Level of interest" # direct/indirect
    # votes:
    #   label: "Number of votes"
