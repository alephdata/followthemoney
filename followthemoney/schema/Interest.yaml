Interest:
  label: "Interest"
  extends: Interval
  icon: fa-certificate
  properties:
    position:
      label: "Position"
    role:
      label: "Role"
    # level:
    #   label: "Level of interest" # direct/indirect
    # votes:
    #   label: "Number of votes"
