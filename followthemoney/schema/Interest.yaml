Interest:
  label: "Interest"
  extends: Interval
  icon: fa-certificate
  fuzzy: false
  featured:
    - role
  properties:
    role:
      label: "Role"
    status:
      label: "Status"
    # level:
    #   label: "Level of interest" # direct/indirect
    # votes:
    #   label: "Number of votes"
