Succession:
  label: "Succession"
  plural: "Successions"
  description: "Two entities that legally succeed each other."
  extends: Interest
  matchable: false
  featured:
    - predecessor
    - successor
    - date
  edge:
    source: predecessor
    target: successor
  properties:
    predecessor:
      label: "Predecessor"
      type: entity
      range: LegalEntity
      reverse:
        name: successors
        label: "Successors" 
    successor:
      label: "Successor"
      type: entity
      range: LegalEntity
      reverse:
        name: predecessors
        label: "Predecessors"
