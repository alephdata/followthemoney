Proxy:
  label: "Proxy"
  extends: Interest
  icon: fa-copy
  properties:
    proxy: # someone with signing rights
      label: "Proxy"
      type: entity
      schema: LegalEntity
    organization:
      label: "Organization"
      type: entity
      schema: LegalEntity