---
const { type } = Astro.props;
const values = Array.from(type.values);
---

{
  values.length > 0 && (
    <>
      <h2>Values</h2>
      <table>
        <thead>
          <tr>
            <th>Value</th>
            <th>Label</th>
          </tr>
        </thead>

        <tbody>
          {values.map(([value, label]) => (
            <tr>
              <td>
                <code>{value}</code>
              </td>
              <td>{label}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </>
  )
}
