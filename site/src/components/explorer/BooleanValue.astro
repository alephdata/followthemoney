---
const { value } = Astro.props;
---

<span class:list={[
  'px-2 inline-block rounded-full font-semibold text-sm',
  value && 'bg-green-100 text-green-900',
  !value && 'bg-red-100 text-red-900',
]}>
  {value ? 'Yes' : 'No'}
</span>
